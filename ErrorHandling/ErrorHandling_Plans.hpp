// Oliver Kullmann, 17.2.2006 (Swansea)

/*!
  \file ErrorHandling_Plans.hpp
  \brief Plans for error handling
  \todo The Boost method seems reasonable, so we should transfer it.
  But better using own types.
  \todo Transferring General/ErrorHandling.hpp. Should we use Messages
  instead of strings? Seems reasonable.
  \todo We should use some macro, which adds to the error message
  the line number and file name, compilation time, and other information.
*/

/*!
  \namespace OKlib::ErrorHandling
  \brief Tools for error (and exception) handling
*/

namespace OKlib {
  namespace ErrorHandling {
  }
}

